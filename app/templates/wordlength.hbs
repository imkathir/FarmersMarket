<h4 class="text-primary">Find number of letters in a word</h4>
<form>
  <div class="form-group row">
    <label for="word" class="col-2 col-form-label">Enter the Word </label>
    <div class="col-sm-8">
      {{input type="text" id="word" class="form-control" value=givenWord autofocus=true}}
    </div>
  </div>
  <h4 class="text-orange text-left pl-2">Given word's Length: {{givenWord.length}}</h4>
</form>


{{outlet}}
